<main class="container my-0">
  <div class="custom-container my-0 p-2">
    <div class="row">
      <!-- Coluna Principal (3/4) -->
      <div class="col-md-9">
        
        <div class="custom-box p-4 mt-4 text-start ms-4"> <!-- Aumentei a margem esquerda -->
          <h3>Espaço para compartilhar histórias</h3>
          <p>Vamos lá!!! Conte pra gente uma história...      
          </p>
        </div>

        <div class="custom-box p-4 mt-4 text-start ms-4">
            <h3 class="text-start">Histórias e casos interessantes e relevantes</h3>
            {{#if stories.length}}
              {{#each stories}}
                <div class="card my-2 custom-box p-2"> <!-- Reduzi o padding aqui -->
                  <div class="card-body d-flex p-2"> <!-- Reduzi o padding aqui também -->
                    <!-- Área da Imagem - Largura fixa -->
                    <div class="me-3" style="width: 100px;">
                      {{#if this.image}}
                        <img src="/images/tribute/{{this.image}}" alt="Símbolo do tributo" class="rounded" style="width: 100px; height: 100px; object-fit: cover;">
                      {{else}}
                        <img src="/images/tribute/tribute_caneta.png" alt="Imagem padrão" class="rounded" style="width: 100px; height: 100px; object-fit: cover;">
                      {{/if}}
                    </div>
                    <!-- Área do Conteúdo -->
                    <div class="flex-grow-1 d-flex flex-column">
                      <!-- Data e Autor -->
                      <small class="text-secondary mb-2">Publicado por {{this.name}} em {{formatDate this.createdAt}}.</small>
                      
                      <!-- Mensagem do tributo com texto maior -->
                      <p class="card-text fs-5 flex-grow-1">{{this.message}}</p>
                      
                      <!-- Botões de ação no rodapé -->
                      <div class="d-flex justify-content-end mt-2">
                        <a href="/tribute/edit-tribute/{{this._id}}" class="me-3 text-warning" style="text-decoration: none;">Editar</a>
                        <a href="/delete/{{this._id}}" class="text-danger" style="text-decoration: none;">Apagar</a>
                      </div>
                    </div>
                  </div>
                </div>
              {{/each}}
            {{else}}
              <p>Nenhum tributo adicionado ainda.</p>
            {{/if}}
          </div>


        
      </div>

      <!-- Coluna Lateral (1/4) -->
      <div class="col-md-3">
        <div class="custom-box p-3 mt-4 me-4"> <!-- Aumentei a margem direita -->
          <h4><i class="bi bi-share-fill"></i> Compartilhe</h4>
          <ul class="list-unstyled">
            <li>Convide amigos e familiares para visistar este memorial.</li>
            <li>Mantenha viva na memória as lembranças</li>
          </ul>
        </div>

        <div class="custom-box p-3 mt-3 me-4"> <!-- Aumentei a margem direita -->
          <h4><i class="bi bi-bar-chart-fill"></i> Estatísticas</h4>
          <p><strong>Visitas:</strong> 1234</p> <!-- Pode ser dinâmico no futuro -->
          <p><strong>Tributos:</strong> {{tribute.length}}</p>
        </div>

        <div class="custom-box p-3 mt-3 me-4">
          <h4>Galeria</h4>
          {{#if gallery.photos.length}}
            {{#each gallery.photos}}
              <img src="/images/uploads/{{this}}" class="img-fluid rounded mb-2 custom-photo-size" alt="Foto de {{../firstName}}">
            {{/each}}
          {{else}}
            <p>Nenhuma foto disponível.</p>
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</main>