<main class="container my-0">
  <div class="custom-container my-0 p-2">
    <div class="row">

      <!-- Coluna Principal -->
      <div class="col-md-9">
        <div class="custom-box p-4 my-2 mx-4">
          <h3 class="mb-4"><i class="bi bi-pencil-square me-2"></i>Editar Galeria de Fotos, Áudios e Vídeos</h3>

          <!-- Tabs -->
          <ul class="nav nav-tabs fw-semibold" id="editGalleryTabs" role="tablist">
            <li class="nav-item" role="presentation">
              <a class="nav-link active text-dark" id="tab-fotos" data-bs-toggle="tab" href="#fotos" role="tab">📷 Fotos</a>
            </li>
            <li class="nav-item" role="presentation">
              <a class="nav-link text-dark" id="tab-audios" data-bs-toggle="tab" href="#audios" role="tab">🎧 Áudios</a>
            </li>
            <li class="nav-item" role="presentation">
              <a class="nav-link text-dark" id="tab-videos" data-bs-toggle="tab" href="#videos" role="tab">🎬 Vídeos</a>
            </li>
          </ul>

          <div class="tab-content mt-3" id="editGalleryTabsContent">

            <!-- Fotos -->
            <div class="tab-pane fade show active" id="fotos" role="tabpanel">
              <form action="/memorial/{{slug}}/gallery/upload" method="POST" enctype="multipart/form-data">
                <input type="file" name="file" accept="image/*" class="form-control my-2" required>
                <input type="hidden" name="slug" value="{{slug}}">
                <input type="hidden" name="memorialId" value="{{memorialId}}">
                <input type="hidden" name="userId" value="{{userId}}">
                <div class="text-end">
                  <button type="submit" class="btn btn-primary rounded-2 shadow-sm px-4 py-2">Enviar</button>
                </div>
              </form>

              <div class="row gallery-grid mt-3">
                {{#if gallery.photos.length}}
                  {{#each gallery.photos}}
                    <div class="col-sm-6 col-md-4 col-lg-3 mb-4 position-relative">
                      <a href="/memorials/{{../slug}}/photos/{{this}}" target="_blank">
                        <img src="/memorials/{{../slug}}/photos/{{this}}" class="galeria-foto rounded shadow-sm img-fluid" alt="Foto do memorial {{../slug}}">
                      </a>
                      <form action="/memorials/{{../slug}}/gallery/delete/photo" method="POST" class="delete-form">
                        <input type="hidden" name="fileName" value="{{this}}" />
                        <button type="submit" class="btn btn-sm btn-danger position-absolute top-0 end-0 m-1 shadow-sm">
                          <i class="bi bi-trash-fill"></i>
                        </button>
                      </form>
                    </div>
                  {{/each}}
                {{else}}
                  <p class="text-muted">Nenhuma foto disponível.</p>
                {{/if}}
              </div>
            </div>

            <!-- Áudios -->
            <div class="tab-pane fade" id="audios" role="tabpanel">
              <form action="/memorial/{{slug}}/gallery/upload" method="POST" enctype="multipart/form-data">
                <input type="file" name="file" accept="audio/*" class="form-control my-2" required>
                <input type="hidden" name="slug" value="{{slug}}">
                <input type="hidden" name="memorialId" value="{{memorialId}}">
                <input type="hidden" name="userId" value="{{userId}}">
                <div class="text-end">
                  <button type="submit" class="btn btn-primary rounded-2 shadow-sm px-4 py-2">Enviar</button>
                </div>
              </form>

              <div class="row mt-3">
                {{#if gallery.audios.length}}
                  {{#each gallery.audios}}
                    <div class="col-md-6 mb-3 position-relative">
                      <audio controls class="media-player w-100 border rounded shadow-sm p-2">
                        <source src="/memorials/{{../slug}}/audios/{{this}}" type="audio/mpeg" />
                        Seu navegador não suporta áudio.
                      </audio>
                      <form action="/memorials/{{../slug}}/gallery/delete/audio" method="POST" class="delete-form">
                        <input type="hidden" name="fileName" value="{{this}}" />
                        <button type="submit" class="btn btn-sm btn-danger position-absolute top-0 end-0 m-1 shadow-sm">
                          <i class="bi bi-trash-fill"></i>
                        </button>
                      </form>
                    </div>
                  {{/each}}
                {{else}}
                  <p class="text-muted">Nenhum áudio disponível.</p>
                {{/if}}
              </div>
            </div>

            <!-- Vídeos -->
            <div class="tab-pane fade" id="videos" role="tabpanel">
              <form action="/memorial/{{slug}}/gallery/upload" method="POST" enctype="multipart/form-data">
                <input type="file" name="file" accept="video/*" class="form-control my-2" required>
                <input type="hidden" name="slug" value="{{slug}}">
                <input type="hidden" name="memorialId" value="{{memorialId}}">
                <input type="hidden" name="userId" value="{{userId}}">
                <div class="text-end">
                  <button type="submit" class="btn btn-primary rounded-2 shadow-sm px-4 py-2">Enviar</button>
                </div>
              </form>

              <div class="row mt-3">
                {{#if gallery.videos.length}}
                  {{#each gallery.videos}}
                    <div class="col-md-6 mb-4 position-relative">
                      <video controls class="media-player w-100 rounded shadow-sm">
                        <source src="/memorials/{{../slug}}/videos/{{this}}" type="video/mp4" />
                        Seu navegador não suporta vídeos.
                      </video>
                      <form action="/memorials/{{../slug}}/gallery/delete/video" method="POST" class="delete-form">
                        <input type="hidden" name="fileName" value="{{this}}" />
                        <button type="submit" class="btn btn-sm btn-danger position-absolute top-0 end-0 m-1 shadow-sm">
                          <i class="bi bi-trash-fill"></i>
                        </button>
                      </form>
                    </div>
                  {{/each}}
                {{else}}
                  <p class="text-muted">Nenhum vídeo disponível.</p>
                {{/if}}
              </div>
            </div>

          </div>
        </div>
      </div>

      <!-- Coluna Lateral -->
      <div class="col-md-3">
        <div class="custom-box-sidebar p-3">
          <h4 class="fw-semibold"><i class="bi bi-share-fill me-2"></i>Compartilhe</h4>
          <ul class="list-unstyled small">
            <li>Convide amigos e familiares para visitar este memorial.</li>
            <li>Mantenha viva na memória as lembranças.</li>
          </ul>
        </div>

        <div class="custom-box-sidebar my-2 mx-2 p-3">
          <h4 class="fw-semibold"><i class="bi bi-bar-chart-fill me-2"></i>Estatísticas</h4>
          <p><strong>Visitas:</strong> 1234</p>
          <p><strong>Tributos:</strong> {{tribute.length}}</p>
        </div>

        <div class="custom-box-sidebar my-2 mx-2 p-3">
          <h4 class="fw-semibold"><i class="bi bi-images me-2"></i>Galeria</h4>
          {{#if gallery.photos.length}}
            <div class="row g-2">
              {{#each gallery.photos}}
                <div class="col-6">
                  <img src="/memorials/{{../slug}}/photos/{{this}}" class="img-fluid rounded shadow-sm custom-sidebar-photo" alt="Foto de {{../firstName}}">
                </div>
              {{/each}}
            </div>

          {{else}}
            <p class="text-muted">Nenhuma foto disponível.</p>
          {{/if}}
        </div>
      </div><!-- Fim da Coluna Lateral -->

    </div>
  </div>
</main>
