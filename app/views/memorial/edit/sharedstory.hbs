<main class="container my-0">
  <div class="custom-container my-0 p-2">
    <div class="row">
      
      <!-- Coluna Principal (3/4) -->
      <div class="col-md-9 my-4">
        <div class="custom-box p-4 ms-4">
          <!-- Formulário de Edição de História Compartilhada -->
          <form action="/sharedstory/update/{{sharedStory._id}}?_method=PUT" method="POST" enctype="multipart/form-data">
            <input type="hidden" name="_method" value="PUT">
            <input type="hidden" name="memorial" value="{{sharedStory.memorial}}" />
            <input type="hidden" name="slug" value="{{slug}}" />

            <div class="custom-box my-2 mx-4">
              <h3>Editar História Compartilhada</h3>
              <div class="card p-3">

                <!-- Título da História -->
                <div class="mb-2">
                  <label for="storyTitle" class="form-label">
                    <i class="bi bi-bookmark-fill"></i> Título da História
                  </label>
                  <input
                    type="text"
                    class="form-control"
                    id="storyTitle"
                    name="title"
                    value="{{sharedStory.title}}"
                    required
                  >
                </div>

                <!-- Conteúdo da História -->
                <div class="mb-2">
                  <label for="storyContent" class="form-label">
                    <i class="bi bi-chat-left-text"></i> Conteúdo
                  </label>
                  <textarea
                    class="edit-input form-control"
                    rows="5"
                    id="storyContent"
                    name="content"
                    required
                  >{{sharedStory.content}}</textarea>
                </div>

                <!-- Data do Acontecimento -->
                <div class="mb-2">
                  <label for="eventDate" class="form-label">
                    <i class="bi bi-calendar-event"></i> Data do Acontecimento
                  </label>
                  <input
                    type="date"
                    class="form-control"
                    id="eventDate"
                    name="eventDate"
                    value="{{eventDate}}"
                  >
                </div>

                <!-- Imagem (opcional) -->
                <div class="mb-2">
                  <label for="storyImage" class="form-label">
                    <i class="bi bi-image"></i> Substituir imagem
                  </label>
                  <input
                    type="file"
                    class="form-control"
                    id="storyImage"
                    name="image"
                    accept="image/*"
                  >
                  {{#if sharedStory.image}}
                    <div class="mt-2">
                      <strong>Imagem atual:</strong><br>
                      <img src="/images/uploads/{{sharedStory.image}}" alt="Imagem da história compartilhada" class="img-fluid rounded mt-2" style="max-height: 200px;">
                    </div>
                  {{/if}}
                </div>

                <!-- Informação de autoria -->
                <div class="text-muted mb-2 fs-7 text-end">
                  Editando como colaborador do memorial.
                </div>

                <!-- Botões -->
                <div class="d-flex justify-content-end align-items-center gap-2">
                  <a href="/memorial/{{slug}}/sharedstory" class="btn btn-secondary">Cancelar</a>
                  <button type="submit" class="btn btn-primary">Atualizar História</button>
                </div>
              </div>
            </div>
          </form>
        </div>
      </div>

      <!-- Coluna Lateral (1/4) -->
      <div class="col-md-3">
        <div class="custom-box p-3 mt-4 me-4">
          <h4><i class="bi bi-people-fill"></i> Contribua com Memórias</h4>
          <ul class="list-unstyled">
            <li>Histórias compartilhadas enriquecem o legado do homenageado.</li>
            <li>Ajude a manter viva essa lembrança.</li>
          </ul>
        </div>

        <div class="custom-box p-3 mt-3 me-4">
          <h4><i class="bi bi-bar-chart-fill"></i> Estatísticas</h4>
          <p><strong>Compartilhamentos:</strong> {{memorial.sharedStories.length}}</p>
        </div>

        <div class="custom-box p-3 mt-3 me-4">
          <h4>Galeria</h4>
          {{#if gallery.photos.length}}
            {{#each gallery.photos}}
              <img src="/images/uploads/{{this}}" class="img-fluid rounded mb-2 custom-photo-size" alt="Foto de {{../firstName}}">
            {{/each}}
          {{else}}
            <p>Nenhuma foto disponível.</p>
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</main>
